[package]
name = "signal-desktop-core"
version = "0.2.0"
description = "Signal Desktop reader — recover the SQLCipher key from config.json via the OS Safe Storage, open sql/db.sqlite, and parse messages, conversations, contacts, and attachment metadata into typed records + a timeline."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
categories.workspace = true
readme.workspace = true

[dependencies]
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
hex = { workspace = true }
rusqlite = { workspace = true }
forensicnomicon-core = { workspace = true }
chromium-safestorage-core = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
aes = { workspace = true }
cbc = { workspace = true }
cipher = { workspace = true }
# Differential test: mint a SQLCipher DB with the reader's own bundled SQLCipher
# and parse the independent `sqlcipher` CLI's JSON output to reconcile against.
rusqlite = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
